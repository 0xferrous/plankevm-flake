name: Update flake lock

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    name: Update plank-monorepo input
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - uses: cachix/install-nix-action@v31
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes

      - name: Update plank-monorepo flake input
        run: |
          echo "::group::Update plank-monorepo flake input"
          nix flake update plank-monorepo --print-build-logs
          echo "::endgroup::"

      - name: Verify updated flake
        run: |
          echo "::group::nix flake check"
          nix flake check --print-build-logs
          echo "::endgroup::"

          echo "::group::Build plank"
          nix build .#plank --no-link --print-build-logs
          echo "::endgroup::"

          echo "::group::Build tree-sitter-plank"
          nix build .#tree-sitter-plank --no-link --print-build-logs
          echo "::endgroup::"

      - name: Commit and push changes
        run: |
          if git diff --quiet flake.lock; then
            echo "flake.lock is already up to date"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add flake.lock
          git commit -m "chore: update plank-monorepo flake input"
          git push origin HEAD:main
